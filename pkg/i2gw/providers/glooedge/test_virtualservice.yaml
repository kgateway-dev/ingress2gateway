apiVersion: gateway.solo.io/v1
kind: VirtualService
metadata:
  name: example-vs
  namespace: default
spec:
  hosts:
    - example.com
  virtualHost:
    routes:
      - matchers:
          - prefix: /api
        routeAction:
          single:
            upstream:
              name: my-service
              namespace: default